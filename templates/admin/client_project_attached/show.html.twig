{% extends 'admin/admin-template.html.twig' %}

{% block title %}ClientProjectAttached{% endblock %}

{% block content %}
    <h1>ClientProjectAttached</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_project_attached.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ client_project_attached.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_client_project_attached_index') }}">back to list</a>

    <a href="{{ path('app_admin_client_project_attached_edit', {'id': client_project_attached.id}) }}">edit</a>

    {{ include('admin/client_project_attached/_delete_form.html.twig') }}
{% endblock %}
