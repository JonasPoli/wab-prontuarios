<form method="post" action="{{ path('app_admin_client_project_attached_delete', {'id': client_project_attached.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_project_attached.id) }}">
    <button class="btn btn-sm btn-outline-danger">Delete</button>
</form>
