<form method="post" action="{{ path('app_admin_client_project_history_delete', {'id': client_project_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_project_history.id) }}">
    <button class="btn btn-outline-danger h:text-white">Deletar</button>
</form>
