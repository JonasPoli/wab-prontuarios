
<form method="post" action="{{ path('app_admin_client_project_delete', {'id': client_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_project.id) }}">
    <button class="btn btn-outline-danger h:text-white">Deletar</button>
</form>
