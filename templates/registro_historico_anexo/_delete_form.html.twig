<form method="post" action="{{ path('app_registro_historico_anexo_delete', {'id': registro_historico_anexo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registro_historico_anexo.id) }}">
    <button class="btn">Delete</button>
</form>
